services:
  tbls:
    image: ghcr.io/wuweiflow/tbls-fms:v0.03
    env_file:
      - .env
    working_dir: /work
    volumes:
      - ./.tbls.yml:/work/.tbls.yml:ro
      - ./docs:/work/docs
    command:
      - doc
      - --force
